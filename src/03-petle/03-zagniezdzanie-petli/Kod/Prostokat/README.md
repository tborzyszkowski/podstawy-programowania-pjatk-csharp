# Projekt: prostokąt znaków

Program demonstruje pętlę zewnętrzną dla wierszy i wewnętrzną dla kolumn. Zbuduj i uruchom go poleceniami `dotnet build` oraz `dotnet run`. Sprawdź przypadek pustego prostokąta i ustaw punkt przerwania w pętli wewnętrznej.
